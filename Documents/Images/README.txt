Place the V-model image file here with the exact name: V-model.jpeg

Recommended size: large enough for 0.8\textwidth display (e.g. 1200px wide). Supported formats for pdflatex: JPEG, PNG.

After adding the image, recompile `Handbook.tex` with `pdflatex`.
